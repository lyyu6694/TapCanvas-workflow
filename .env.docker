## TapCanvas Docker defaults (committed)
## Used by docker-compose to inject runtime env into containers.

# Disable proxy by default (avoid localhost proxy inside containers)
DISABLE_PROXY=1

# Faster npm registry mirror (override if needed)
NPM_REGISTRY=https://registry.npmmirror.com

# Docker Hub mirror prefix for images used in compose (override if needed)
DOCKERHUB_REGISTRY=docker.1ms.run

# Web runtime config (Vite will expose VITE_* to the browser)
# Production: replace with your actual API domain
VITE_API_BASE=https://api.your-domain.com
VITE_LANGGRAPH_API_URL=http://localhost:8123
VITE_LANGGRAPH_API_BASE=http://localhost:8123
VITE_LANGGRAPH_ENABLED=1
VITE_WEBCUT_URL=http://localhost:5174

# Auth config (email verification replaces GitHub OAuth)
JWT_SECRET=change-me-to-a-secure-random-string
SMTP_163_USER=your-163-email@163.com
SMTP_163_PASS=your-smtp-authorization-code
ADMIN_EMAILS=admin@example.com
LOGIN_URL=https://your-domain.com

# LangGraph API CORS (for LangSmith Studio + local web + production)
# Comma-separated list. Override locally if needed.
CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://your-domain.com,https://smith.langchain.com
OPENAI_MODEL=gpt-4
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_BASE_URL=https://api.openai.com/v1



